name: .NET

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Restore dependencies
      run: dotnet restore ThePage/ThePage.sln
    - name: Build
      run: dotnet build ThePage/ThePage.sln --no-restore
    - name: Test
      run: dotnet test ThePage/ThePage.sln --no-build --verbosity normal
